# Django Settings
DEBUG=False
SECRET_KEY=django-strong-secret-key
ALLOWED_HOSTS=*

# Database - TimescaleDB
DB_NAME=meteodb
DB_USER=infoclimat
DB_PASSWORD=infoclimat2026
DB_HOST=timescaledb
DB_PORT=5432

# CORS
CORS_ALLOWED_ORIGINS=https://domain.com

# Frontend
NUXT_PUBLIC_API_BASE=/api/v1

# Production image tag (set to a specific git SHA or semver tag)
IMAGE_TAG=latest

# Domain (used by Nginx TLS and Grafana root URL)
DOMAIN=domain.com

# Grafana
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
